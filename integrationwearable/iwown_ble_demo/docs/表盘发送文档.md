# 				                                                protobuf表盘发送

### * 前置条件

   	通过python 项目dial_resource用来生成移动端需要 的必要文件，dial_resource放到服务器上， config.json 文件 和 proto.bin 通过网络请求获取。config.json内容中包含图片等必要内容，proto..bin文件则需要直接转成byte字节发送手表。以下是 config.json的示例

```java
//服务器上的imgs 应该是: ["https://xxxx.bg-1.png","https://xxxx.time-0.png",******]

{
	"imgs": ["bg-1.png", "time-0.png", "time-1.png", "time-2.png", "time-3.png", "time-4.png", "time-5.png", "time-6.png", "time-7.png", "time-8.png", "time-9.png", "ele-BAT-icon-1.png", "state-AF-1.png"],
	"ts": "1705054675",
	"size": 333217,
	"editable": 0.0,
	"effect_graph": {
		"width": 368,
		"height": 448,
		"shape": "S"
	}
}

```

### * android下发表盘流程

 *参考 DialDemoActivity

###     // 同步数据或者在下发agps时候不要发送表

##### 	1.  通过网络下载config.json 和 proto.bin文件(demo 中内置了一个表盘的这两个文件)

#####     2. 下载json文件里面的imgs的所有图片，并且转为byte数组（demo中内置一个表盘的图片）,所有图片加起来最大512KB, 所以图片都需要压缩。调用 *DialUtils.allPictureToProtoBytes( xxx )* 得到byte数组

```java
//以下是 如何将所有图片转为bytes，如果需要自定义表盘背景 ，将 bg-1.jpg换成其他图片路径即可
//最新的手表 背景支持 .jpg ,其他元素支持 .png

/**
* 将所有图片转为bytes
* 如果需要自定义表盘背景 ，将 bg-1.jpg换成其他图片路径即可
*/
fun allPictureToProtoBytes(filePathList:MutableList<String>,bgConvert:Boolean = false,otherConvert:Boolean = false):ByteArray{}

```
#####    3. 发送清除表盘的指令（同步数据和升级agps时不下发表盘）, 并接受设备的回应（0x0E）

   ```java
//清除表盘
byte[] bytes1 = ProtoBufSendBluetoothCmdImpl.getInstance().clearDeviceDial();
BackgroundThreadManager.getInstance().addWriteData(BleApplication.getInstance(),bytes1);

//设备的回应，返回dataType = 0x0E指令 
    private static void parse0EData(Context context, String data) {
        ProtoCustomCode customCode = JsonTool.fromJson(data,ProtoCustomCode.class);
				 if(customCode.getCode()==2 && customCode.isRetType()){
            //清除表盘成功
            DialProgressEvent progressEvent = new DialProgressEvent(true);
            EventBus.getDefault().post(progressEvent);
        }
    }
      

   ```

   demo示例

```kotlin
// 调用 ProtoBufSendDialImpl.setProtoBufDial() 判断是否可以下发，返回true时，清除指令以自定发送

    private fun setProtoBufDial(): Boolean{
        when (ProtoBufUpdate.getInstance().startUpdateDial()) {
            1 -> {
                //断连了
                iBaseDialProgress.writeDialHasStop(DialWriteStopType.BLE_DISCONNECT)
                return false
            }
            2 -> {
                //正在同步数据(agps也是同步的一部分)
                iBaseDialProgress.writeDialHasStop(DialWriteStopType.SYNC)
                return false
            }
            else -> {
                //正在写入表盘，进度通过eventBus返回
                iBaseDialProgress.onDownloadStart()
                iBaseDialProgress.onProgress(ProgressType.WRITE, 0)
                return true
            }
        }
    }

```

#####   4. 清除指令回应后，将第2步获取到的图片byte数组发送给设备，并接受表盘进度（0x90）

 ```kotlin
ProtoBufUpdate.getInstance().beginWriteDial(fileByte)

//demo示例

    /**
     * protobuf写入文件返回的进度条
     */
    @Subscribe(threadMode = ThreadMode.MAIN)
    fun onEventMainThread(writeEvent: DialProgressEvent) {
        if(writeEvent.isClear){
            //清除回应，下发指令 必须在清除指令之后发送
            ProtoBufUpdate.getInstance().beginWriteDial(fileByte)
            return
        }

        iBaseDialProgress.onProgress(ProgressType.WRITE, writeEvent.progress)
        if(writeEvent.isOk){
            //表盘已经写入结束
            iBaseDialProgress.writeDialHasStop(DialWriteStopType.SUCCESS)
        }else if(writeEvent.progress == 100){
            iBaseDialProgress.onDownloadFinish()
            handler.removeCallbacksAndMessages(null)
            handler.postDelayed({
                val cmd = ProtoBufSendBluetoothCmdImpl.getInstance().setCustomDial(pbBin,1)
                BackgroundThreadManager.getInstance().addWriteData(Utils.getApp(),cmd)
            }, 600)
        }
    }

 ```

#####   5. 图片bytes全部发完后，progress==100时，将proto..bin 转为字节发送给设备

```kotlin

val pbBin = FileIOUtils.readFile2BytesByMap(binPath)

val cmd = ProtoBufSendBluetoothCmdImpl.getInstance().setCustomDial(pbBin,1)
BackgroundThreadManager.getInstance().addWriteData(Utils.getApp(),cmd)

//demo示例在第4步中
```

#####   6. proto.bin 发送给设备后，设备回应 0x0E指令，内容中是成功表示表盘发送成功，流程结束

```java
    private static void parse0EData(Context context, String data) {
        ProtoCustomCode customCode = JsonTool.fromJson(data,ProtoCustomCode.class);
        if(customCode.getCode()==0){
            //写入表盘成功
            DialProgressEvent progressEvent = new DialProgressEvent(0);
            progressEvent.setOk(true);
            EventBus.getDefault().post(progressEvent);
        }
    }
```








